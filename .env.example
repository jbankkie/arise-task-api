# Database Configuration for Local Development
PORT=8080
GIN_MODE=debug

# Local Database Connection (when PostgreSQL runs in Docker)
DB_HOST=172.19.18.36
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=taskmanager

# Security
JWT_SECRET=your-development-secret-key
